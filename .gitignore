# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

data/south-florida-agents.txt
logo horrible.png
agent.agencies USA/

# Florida OFR PRR originals / extracts (immutable local copies)
data/raw/

# FL-LEND-002D private person/MLO staging (never commit)
data/generated/fl-lend-002d/private/

# Raw HMDA LAR extracts (multi-GB) — never commit
year_2025.csv
state_FL-*.csv
*.hmda-raw.csv

# Optional local caches / extracts (regenerated by scripts)
data/hmda/florida/_our_lenders_extract.json

# National lender×county file can exceed GitHub soft limits — keep local if huge
# (smaller national tables + by-state partitions are committed)
data/hmda/national/lender_activity_by_county.csv
